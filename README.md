Um die `.exe` zu erstellen muss folgender Befehl im Ordner der `program.cs`-Datei ausgeführt werden: `python -m PyInstaller --onefile --windowed program.py`.

Der `.exe` befinet sich dann im Ordner `.dist`.
